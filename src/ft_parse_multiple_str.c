int	ft_parse_multiple(char **av)
{
	if (!av[1][0])
		return (0);
	return (1);
}

	/*if (ft_same(tmp) && ft_is_int(tmp))
	{
		free_split(tmp);
		return (1);
	}
	else
	{
		free_split(tmp);
		return (0);
	} */
